<!--------------------------------- NAVBAR -------------------------------->
    <nav> 
      <div class="top"> <!--------------- top ---------------->
        <div class="top-control">
          <% if user_signed_in? %>
            <%= link_to 'Se déconnecter', destroy_user_session_path, method: :delete %> <br>
            <%= current_user.email %>
          <% else %>
            <%= link_to 'Se créer un compte', new_user_registration_path %><span>•</span><%= link_to 'Se connecter', new_user_session_path %>
          <% end %>
        </div>
        <div class="clearfix">
          <!-- purple nav-bar -->
        </div>
      </div><!------------------ top -------------------->

      <div id="believe-nav"> <!------ white nav-bar ------->
        <div class="container">
          <div class="min-marg">
            <nav class="navbar navbar-default">
              <div class="navbar-header">
                <a class="navbar-brand" href="index.html">
                  <%= image_tag('logo.png', alt: "") %>
                </a>
              </div>
              <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                  <li class="active">
                      <%= link_to 'Acceuil', root_path %>
                  </li>
                  <li>
                    <% if user_signed_in? %>
                      <%= link_to 'Mon profil', new_user_registration_path %>
                    <% end %>
                  </li>
                </ul>
                
                <ul class="nav navbar-nav navbar-right">
                  <li>
                    <a href="#">
                      <%= image_tag('bag.png', alt: "bag") %>
                    <span>2</span></a>
                  </li>
                </ul>
              </div>
            </nav>
          </div>
        </div>
      </div> <!-------------- end white nav-bar ------------->
    </nav> 
<!--------------------------------- END NAVBAR ------------------------------------->